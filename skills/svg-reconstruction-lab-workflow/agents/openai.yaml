interface:
  display_name: "SCI Draw.io Figure Workflow"
  short_description: "Refine editable SCI figures with SVG math"
  brand_color: "#334F6B"
  default_prompt: "Use $svg-reconstruction-lab-workflow to refine this draw.io academic figure with SVG formulas, teacher-feedback edits, and a high-resolution PNG export."

policy:
  allow_implicit_invocation: true
